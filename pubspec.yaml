name: checklist
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  uuid: ^2.0.2
  rxdart: ^0.22.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
